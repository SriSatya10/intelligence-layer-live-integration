# Build State
Most repositories are pure Python environments requiring only `pip install -r requirements.txt`. No complex build steps (like Docker or compiled code) are currently required. Tests run via `pytest`.
